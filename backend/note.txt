create session
get Turn 
get random category
select a category -> get three random question
create round
submit answer 
end round
get turn 
based on whose turn -> create round and select category


...
sessions
get sessions (for admin all , for user his)
get session
create session
delete session (for admin)

rounds 
create round 
get rounds (for admin all , for user his)
get round 
delete round (for admin)

turn 
get turn 

random 
get random category
get random question



--------scenario
/api/session/user/user_id
/api/round/turn/session_id/session_id
/api/category/random/random
/api/question/radnom/category_id
/api/round/
    {
        "session_id"
        "category_id"
        "round_questions"
    }
/api/round/submit
    {
        "session_id"
        "round_id"
        "question_id"
        "user_id"
        "answer"
    }


----------------
admin should see the sessions and rounds
players should see player stats and leaderboard

